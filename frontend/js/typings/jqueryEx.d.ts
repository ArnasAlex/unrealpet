interface JQuery {
    ajaxSubmit(options: any);
    fileupload(options: any);
    jPlayer(options: any);
    jPlayer(method: string, options: any);
    velocity: Function;
}

interface JQueryStatic {
    mockjax(options: any);
}

interface JQuerySupport {
    fileInput: any;
}
